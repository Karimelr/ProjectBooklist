{% extends 'base.html.twig' %}

{% block title %}BOOKLIST! Voor al uw informatie over boeken{% endblock %}

{% block body %}
    <br>
    <h1>Boek index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>ISBN</th>
                <th>Auteur</th>
                <th>Titel</th>
                <th>Prijs</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for boek in boeks %}
            <tr>
                <td>{{ boek.id }}</td>
                <td>{{ boek.ISBN }}</td>
                <td>{{ boek.Auteur }}</td>
                <td>{{ boek.Titel }}</td>
                <td>{{ boek.Prijs }}</td>
                <td>
                    <a href="{{ path('boek_show', {'id': boek.id}) }}">show</a>
                    <a href="{{ path('boek_edit', {'id': boek.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('boek_new') }}">Create new</a>
{% endblock %}
